# 电商财务报表改造目标

> 本文件记录“电商业务财务核算 4 个附件模板”的数据契约。实施店铺、TikTok Shop 同步、成本归集、财务报表或 Excel 导出前，必须先阅读本文件及 `PROJECT_RUNBOOK.md`。除非另有确认，不能用现有 FIFO 的单一销售成本或平台订单金额直接填充财务报表。

## 目标

系统按核算期间导出一个 Excel，包含以下四张表：

1. `07-存货跌价准备明细表`
2. `09-店铺损益表`
3. `10-各店铺损益汇总表`
4. `12-各产品线毛利分析表`

报表应可追溯到店铺、订单行、库存批次、费用单据或人工录入凭证。导出文件是报表结果，不是业务数据的唯一来源。

## 模板口径

### 07-存货跌价准备明细表

一行代表一个店铺、SKU 和入库批次（或经确认的库存成本层）在报告截止日的剩余库存。输入字段是店铺、SKU、商品名称、入库日期、单件到仓成本和库存数量。

模板规则：

- 已入库天数 = 报告截止日 - 入库日期。
- 每日计提 1%，累计上限 90%。
- 库存总金额 = 单件到仓成本 x 库存数量。
- 累计跌价准备 = 库存总金额 x 累计计提比例。
- 账面价值 = 库存总金额 - 累计跌价准备。
- 与上月末账面价值差额 = 上月末账面价值 - 当前账面价值。

实现时必须使用可选择的报告截止日和已关账的上月快照，不能在最终财务导出中直接使用 Excel `TODAY()`。模板说明中的“单件到仓成本”至少包括采购成本、头程物流和关税；是否包括尾程、本地配送和其他成本需由财务确认。

### 09-店铺损益表

一张单店、单期间的损益表，列为本月、上月、本年累计。营业收入包括商品销售收入、运费收入和其他收入。营业成本拆分为商品采购成本、头程物流成本、关税及进口税费、本地物流配送成本、代发成本和其他直接成本。

营业利润还需扣除税金及附加、人员薪资、广告推广费、平台月费/年费、仓储费用、配送费、平台罚款/赔偿、房租水电网费、分摊人事财务费用、研发费用、财务费用、存货跌价准备和信用减值损失，之后计算营业外收支、所得税和净利润。

### 10-各店铺损益汇总表

行口径与店铺损益表一致，列为各店铺、合计及占比。它必须由已经核算完成的每店损益汇总，不能重复以另一套规则计算。毛利率应使用 `总毛利 / 总营业收入`，不能对各店毛利率简单求和。

### 12-各产品线毛利分析表

一行代表一个产品线或品类，跨店汇总 SKU 数量、本月收入、本月成本、毛利、毛利率、上月毛利率、环比、本年累计收入、本年累计毛利和累计毛利率。产品线必须是系统内受控主数据，不能只依赖商品名称的文本匹配。

## 现有 FIFO 项目对应关系

| 报表需要的数据 | 当前来源 | 当前状态 |
| --- | --- | --- |
| SKU、商品名称、图片 | `products` | 已有，但没有店铺商品映射和产品线 |
| 入库日期、剩余数量 | `inventory_batches.arrived_at`、`remaining_quantity` | 已有，但没有店铺/仓库归属及历史期末快照 |
| 采购成本、头程、尾程、其他成本 | `inventory_batches` | 已有整批金额，但没有关税字段，且销售明细只保存总单件成本 |
| 销售数量、订单号、销售时间 | `sales` | 已有手工录入，缺少店铺、平台订单状态、退款和结算信息 |
| FIFO 销售成本 | `sale_cost_details` | 已有总单件成本，无法按采购、头程、关税、本地配送等财务科目拆分 |
| 月度商品销量和当前库存 | `/reports/monthly` | 仅有商品层统计，不能用于店铺损益和关账报表 |
| 费用、税费、广告、平台账单 | 无 | 必须新增费用/结算事实数据 |

## 必须新增的业务对象

1. `stores`：平台、TikTok Shop 店铺 ID、店铺名称、国家或区域、结算币种、时区、法人主体、启停状态。
2. `store_products`：店铺商品 ID、店铺 SKU 或 SKU ID、本地 `products` 商品、产品线、有效期。一个本地 SKU 可以映射多个店铺。
3. `product_lines`：受控产品线或品类主数据。
4. `platform_orders` 与 `platform_order_lines`：店铺订单、平台订单号、状态时间、原始金额、买家运费、折扣、税、退款和售后状态；每行关联店铺商品。
5. `settlement_entries`：按平台账单或结算流水保存收入、佣金、支付费、物流费、罚款、补贴、退款等原始科目及结算日期，禁止仅以订单展示金额替代实际结算。
6. `expense_entries`：广告、薪资、租赁水电、仓储、研发、财务费用、税金、营业外收支等非订单费用。每笔须包含期间、店铺或分摊规则、币种、金额、科目、来源凭证和审核状态。
7. `inventory_snapshots` 或可重建的库存流水：按报告截止日冻结店铺、SKU、批次的数量和成本，支持月末重跑与审计。
8. `accounting_periods`：报告期间、时区、关账状态和快照版本；关闭期间不应被后续订单同步覆盖。
9. `platform_connections` 与 `sync_runs`：授权店铺、加密令牌、授权范围、最近同步水位、同步结果和错误。密钥和令牌不得写入 Git、日志或 Excel。

## TikTok Shop 接入结论

TikTok Shop Partner Center 提供面向应用的授权和 API 权限管理，接入应采用每个商家店铺授权的方式，而不是保存商家账号密码。订单、商品、履约/售后和结算数据应分别同步，并保留平台原始 ID 及原始金额字段。公开页面无法在未登录且未选择应用权限时确认某个店铺区域的具体财务接口字段，因此结算数据的可得性必须在 Developer Center 为目标店铺申请并授权后，以该应用可见的最新 API 文档为准。

官方入口：

- `https://partner.tiktokshop.com/`
- `https://partner.tiktokshop.com/docv2/`

接入前需要用户提供或完成：

1. 应用类型（自用应用或服务商应用）、目标站点/国家和每个店铺的授权范围。
2. TikTok Shop Developer Center 创建的应用凭据，以及安全的生产环境密钥保存方式。
3. 至少一个已授权测试店铺，用于确认订单、退款、结算和商品字段。
4. TikTok Shop 对结算账单/财务流水 API 的实际权限。若该区域或账户无对应权限，必须改为稳定的官方报表文件导入，不能伪造或抓取后台页面。
5. 广告数据来源。广告推广费通常需要独立的 TikTok Ads 授权与接口，不能假定 Shop API 会返回。

## 待确认的财务口径

当前销售导入已采用人民币作为页面核算币种：外币按订单日期使用 Frankfurter/ECB 历史汇率换算，非交易日取之前最近交易日，并固化汇率、实际汇率日期和来源。最终四张财务报表是否沿用该来源和舍入规则，仍需在关账口径确认时正式批准。

1. 商品销售收入按付款、发货、妥投、完成还是结算日期确认？金额是订单原价、折后实收、含税还是不含税？
2. 买家支付运费进入“运费收入”还是冲减配送费？平台补贴、商家折扣和平台折扣分别如何确认？
3. 退款、部分退款、拒付、取消和平台赔付在当期如何冲回收入、成本和费用？
4. 关税及进口税费是否必须独立录入并分摊至批次？当前 `other_cost` 不能替代该科目。
5. 尾程派送、本地物流配送成本、销售费用中的“配送费”如何区分，避免重复计入？
6. 存货跌价准备是否严格执行模板的每日 1%、90% 上限，并以批次、SKU、店铺还是仓库为计提单元？
7. 所有店铺的记账本位币是什么？若存在 MXN、USD 等币种，汇率来源、换算日期和舍入规则是什么？
8. 一个 SKU 是否可能在多店铺共享库存或跨店调拨？库存归属和成本如何分配？
9. 历史数据从哪个日期开始纳入新报表？现有批次的采购、头程、尾程、其他成本是否需要补录或按规则拆分？

## 建议实施顺序

1. 固化上述财务口径和一份真实 TikTok Shop 导出样本。
2. 新增店铺、产品线、店铺商品映射、期间和成本拆分，不改写已关闭历史数据。
3. 建立订单、退款、结算流水和费用台账的幂等导入层，先支持文件导入，再接 TikTok Shop API。
4. 增加成本分层和月末库存快照，确保库存跌价准备及营业成本可审计。
5. 实现单店损益、跨店汇总和产品线毛利计算，增加勾稽校验。
6. 以该模板的四张表导出 Excel，并用真实月度数据与财务人工报表逐项对账。

## 验收标准

- 同一报告期间可重复导出并得到相同结果。
- 每个收入、成本和费用金额能追溯到订单行、结算流水、库存批次或费用凭证。
- 退款和结算调整不会重复记账。
- 各店损益合计等于汇总表合计，产品线收入和营业成本可与店铺报表按既定范围对账。
- 月末库存数量、库存成本、跌价准备和损益表资产减值损失可勾稽。
- 未授权 API、缺失汇率、未映射 SKU、未分类费用和未关账期间必须明确提示，不能静默按零处理。
